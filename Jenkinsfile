node () { 
  echo "Branch is NO-126"
  sh "netstat -a" 
  echo "Pre-migration change to NO-126"
  echo "Post-migration change to NO-126"
  echo "If this works right it should not build automatically"
}
